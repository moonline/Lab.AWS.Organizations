AWSTemplateFormatVersion: 2010-09-09
Transform: AWS::Serverless-2016-10-31

Description: Organization with AWS Organizations, Identity Center and SCPs

Parameters:
  environment:
    Type: String
    Default: prod
    AllowedValues:
      - dev
      - test
      - prod
    Description: Deployment environment, e.g. dev for development
  identityCenterInstanceArn:
    Type: String
    AllowedPattern: (^$|^arn:aws:sso:::instance/(sso)?ins-[a-zA-Z0-9-.]{16})
    Description: >
      ARN of an IAM Identity Center, e.g. 'arn:aws:sso:::instance/ssoins-xxxxxxxxxxxxxxxx'.
      Find by `aws sso-admin list-instances`.
    Default: ""
  organizationEmail:
    Type: String
    AllowedPattern: ^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$
    Description: >
      An email address like 'aws@your-domain.tld'.
      Each account will get a labeled address like 'aws+the-account-name@your-domain.tld'.
  supportedRegions:
    Type: String
    Description: >
      Comma separated list of supported regions. The first is the main region.
      See https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.RegionsAndAvailabilityZones.html.
      Examples:
      - us-east-1 # US East (N. Virginia)
      - sa-east-1 # South America (São Paulo)
      - eu-central-1 # Europe (Frankfurt)
      - eu-central-2 # Europe (Zurich)
      - ap-southeast-1 # Asia Pacific (Singapore)
      These regions must be enabled in the account: https://us-east-1.console.aws.amazon.com/billing/home?region=eu-central-1#/account?AWS-Regions
    Default: "us-east-1,eu-central-1"

Conditions:
  HasIdentityCenterInstance: !Not [!Equals [!Ref identityCenterInstanceArn, ""]]

Resources:
  OrganizationStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      Parameters:
        environment: !Ref environment
        organizationEmail: !Ref organizationEmail
      Tags:
        - Key: environment
          Value: !Ref environment
      TemplateURL: "./organization.yml"
      TimeoutInMinutes: 10

  CustomResourcesStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      Parameters:
        environment: !Ref environment
        # organizationStackName: organization
        organizationId: !GetAtt OrganizationStack.Outputs.OrganizationId
      Tags:
        - Key: environment
          Value: !Ref environment
      TemplateURL: "./custom-resources/custom-resources.yml"
      TimeoutInMinutes: 10

  LoggingStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      Parameters:
        environment: !Ref environment
        # organizationStackName: organization
        managementAccountId: !GetAtt OrganizationStack.Outputs.ManagementAccountId
        organizationId: !GetAtt OrganizationStack.Outputs.OrganizationId
        organizationsAwsServiceAccessFunctionArn: !GetAtt CustomResourcesStack.Outputs.OrganizationsAwsServiceAccessFunctionArn
      Tags:
        - Key: environment
          Value: !Ref environment
      TemplateURL: "./logging.yml"
      TimeoutInMinutes: 5

  IdentityStack:
    Type: AWS::CloudFormation::Stack
    Condition: HasIdentityCenterInstance
    Properties:
      Parameters:
        environment: !Ref environment
        #organizationStackName: organization
        identityCenterInstanceArn:
          !If [
            "HasIdentityCenterInstance",
            !Ref identityCenterInstanceArn,
            !Ref "AWS::NoValue",
          ]
        organizationRootId: !GetAtt OrganizationStack.Outputs.OrganizationRootId
        supportedRegions: !Ref supportedRegions
        securityAccountId: !GetAtt OrganizationStack.Outputs.SecurityAccountId
        organizationsAwsServiceAccessFunctionArn: !GetAtt CustomResourcesStack.Outputs.OrganizationsAwsServiceAccessFunctionArn
        cloudformationOrganizationsAccessFunctionArn: !GetAtt CustomResourcesStack.Outputs.CloudformationOrganizationsAccessFunctionArn
        organizationsDelegatedAdministratorFunctionArn: !GetAtt CustomResourcesStack.Outputs.OrganizationsDelegatedAdministratorFunctionArn
      Tags:
        - Key: environment
          Value: !Ref environment
      TemplateURL: "./identity/identity.yml"
      TimeoutInMinutes: 10

  ServiceControlPoliciesStack:
    Type: AWS::CloudFormation::Stack
    Properties:
      Parameters:
        environment: !Ref environment
        # organizationStackName: organization
        organizationRootId: !GetAtt OrganizationStack.Outputs.OrganizationRootId
        supportedRegions: !Ref supportedRegions
        organizationsPolicyTypeFunctionArn: !GetAtt CustomResourcesStack.Outputs.OrganizationsPolicyTypeFunctionArn
      Tags:
        - Key: environment
          Value: !Ref environment
      TemplateURL: "./service-control-policies.yml"
      TimeoutInMinutes: 10

Outputs:
  ManagementAccountId:
    Description: Account ID of the organizations management account
    Value: !GetAtt OrganizationStack.Outputs.ManagementAccountId
    Export:
      Name: !Sub "${AWS::StackName}:ManagementAccountId"
  OrganizationId:
    Description: ID of the organization
    Value: !GetAtt OrganizationStack.Outputs.OrganizationId
    Export:
      Name: !Sub "${AWS::StackName}:OrganizationId"
  OrganizationRootId:
    Description: ID of the organization root
    Value: !GetAtt OrganizationStack.Outputs.OrganizationRootId
    Export:
      Name: !Sub "${AWS::StackName}:OrganizationRootId"
  SecurityAccountId:
    Description: ID of the security account
    Value: !GetAtt OrganizationStack.Outputs.SecurityAccountId
    Export:
      Name: !Sub "${AWS::StackName}:SecurityAccountId"
